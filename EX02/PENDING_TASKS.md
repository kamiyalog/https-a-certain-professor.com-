# 保留タスク

## CV・限定確認対応（2026年8月20日）

- 案内人選択画面・クレジットへ、男性 `もこ山モコ`、女性 `鯱子` を反映済み。
- 第一章の初回BAD END後を、最新の案内人収録台本どおり8ボイス構成へ反映済み。
- 最新収録台本の全90ボイスと、ゲーム内の案内人台詞をID単位で照合済み。
- `EP1-010` と `EP4-020` の表示文を最新収録台本へ統一済み。
- 男性WAV `M-INTRO-001.wav` ～ `M-EP4-024.wav` を読み込む再生処理を実装済み。
- CV音声はAIへ取り込まない取り決めのため、WAV自体は本ZIPへ未収録。制作者が `assets/voice/` へ配置する。
- 暫定暗証番号 `0701` の入口を実装済み。一般公開時は `PREVIEW_GATE_ENABLED = false` で解除する。
- 章選択・物語選択画面から開けるボイス確認ページを実装済み。
- ボイス確認ページから場面単位で案内人ADVを呼び出し、終了後に一覧へ戻る処理を実装済み。
- ボイス確認中のファイル名表示、音声再生、一覧復帰を実装済み。

## 画像作成・組み込み

### 1. 病室512・夜

- 現在の画像は、患者部分のキャラクターとイラストのテイストが、ほかの病室512画像と揃っていない。
- 病室の構図・背景は維持し、キャラクター部分を同一人物・同一テイストに作り直す。
- 完成後、現在の `病室512・夜.png` と差し替えてゲームへ組み込む。
- 1回目の候補はテイスト不一致のため不採用。
- 朝版の写実的な画像を直接ベースにした `病室512・夜_同一テイスト修正版候補_v2.png` を正式採用。
- 正式画像 `病室512・夜.png` としてゲームへ組み込み済み。

### 2. 病室512・深夜

- 現在の `病室512・深夜.png` は削除しない。高瀬さんが苦しそうにしている後半の描写で使用する。
- 第3章 `3-1` の次の文章には、別の新規画像が必要。

> 午前3時35分。  
> 512号室の高瀬さんは、呼吸補助マスクをつけたまま目を覚ましていた。

- 新規画像では、呼吸補助マスクを装着して目を覚ましているが、まだ苦しそうには見えない状態にする。
- 完成後、`3-1` では新規画像を表示し、その後の苦しそうな描写では現在の `病室512・深夜.png` を表示するよう、背景指定と画像マッピングを分けて組み込む。
- 覚醒版は正式採用済み。`病室512・深夜.png` として3-1へ組み込み済み。
- 旧深夜画像は `病室512・深夜・問題時.png` に改名し、3-37～3-39へ適用済み。
- 改変側の3-2-8～3-2-17にも `病室512・深夜・問題時.png` を適用済み。
- 3-40～BAD END②は黒背景へ変更済み。

## 状態

- `病室512・夜` と `病室512・深夜` は、画像作成・正式採用・ゲーム反映まで完了。
- CV・限定確認機能の実装は完了。男性WAV90点は制作者側で `assets/voice/` へ配置する。
